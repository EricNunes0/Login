const newReturnButton = document.querySelector("#new-return-button");

newReturnButton.addEventListener("click", () => {
    alterIconInputRemove();
    newIconRemove();
    newIconAreaClose();
    fixedDivOpen();
});